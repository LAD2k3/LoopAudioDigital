@*
@using LoopAudioDigital.Models;
@model ARTISTDeleteViewModel
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
<div class="card">
    <div class="card-body">
        <input asp-for="@Model.Id" readonly />
        <input asp-for="@Model.ArtistName" readonly/>
        <form method="post" asp-action="Delete" asp-controller="Artist">
            <button type="submit" class="btn btn-danger">Yes</button>
            <a asp-action="Index" class="btn btn-info">No</a>
        </form>
    </div>
</div>

@section Scripts{
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
*@
